'use server';

import { LoginFormValues } from '@/components/form/login-form';

export default async function LoginAction(data: LoginFormValues) {
      //validate users data
      // create user in supabase
      // set a cookie for authorization
}
